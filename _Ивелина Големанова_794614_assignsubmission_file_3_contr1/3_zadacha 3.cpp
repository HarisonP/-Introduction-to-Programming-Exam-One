#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	int array_one[10];
	int array_two[10];
	int new_array[20];
	int size = 10;

	for (int i; i < size - 1; i++)
	{
		cin >> array_one[i];
	}

	for (int g; g < size - 1; g++)
	{
		cin >> array_two[g];
	}

	if (array_one[i] != array_two[g])
	{
		
	}

	
	return 0;
}
