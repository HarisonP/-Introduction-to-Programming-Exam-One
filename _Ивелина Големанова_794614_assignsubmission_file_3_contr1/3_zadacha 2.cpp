#include <iostream>
#include <cmath>
using namespace std;
/*
int main()
{
	int number, last_digit, first_digit, difference;
	cin >> number;

	last_digit = number % 10;

	while (number != 0)
	{
		number = number % 10;
		first_digit = number / 10;	
	}

	difference = last_digit - first_digit;


	int digit, new_number;
	while (number != 0)
	{

		int counter;
		counter == 0;

		digit = number % 10;

		if (digit % 3 == 0)
		{
			digit = digit / 3;
		}

		else
		{
			if (digit % 2 == 0)
			{
				digit = digit / 2;
			}

			else
			{
				digit = digit + 2;
			}
		}

		++counter;
		number = number / 10;
		new_number = number + digit * pow(10.0, counter);

	}

	cout << new_number << endl;

	
	return 0;

} */