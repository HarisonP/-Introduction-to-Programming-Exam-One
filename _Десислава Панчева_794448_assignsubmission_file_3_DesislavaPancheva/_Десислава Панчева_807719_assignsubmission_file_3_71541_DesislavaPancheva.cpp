#include <iostream>
#include <string.h>
using namespace std;

int broi(char str[100])
{
	int counter = 0;
	for (int i = 0; i < strlen(str), i++)
	{
		int space = ' ';
		space = strlen(str) - 1;
	}
// return ?


}

// 0.4/0.5


int main()
{/*
	
	int const N =3;
	
	int sum_under= 0;
	
	int sreshtano_chislo = 0;
	int matr[N][N];
	int min = matr[1][0];

	for (int i = 0; i < N; i++)
	{
		for (int j = 0; j < N; j++)
		{
			cout << "matr[ " << i << " ][ " << j << " ]= ";
			cin >> matr[i][j];
		}
	}

	
	for (int i = 0; i < N; i++)
	{
		for (int j = 0; j < N; j++)

			cout << matr[i][j] << "    ";
			cout << endl;
	}

	for (int i = 0; i <N; i++)
	{
		for (int j = 0; j < N; j++)
		{
			if (i + j > N - 1)

				sum_under = sum_under + matr[i][j];

		}
	}


	for (int i = 1; i < N; i + 2)
	{
		for (int j = 0; j < N; j++)
		{
			if (min == matr[i][j])

				sreshtano_chislo=sreshtano_chislo+1;
			
		}
	}




cout << "Sumata na el pod gl diag e" << sum_under << endl;
cout << " sreshtano chislo: " << sreshtano_chislo;
	*/
// 0.9 /2.0

2zad:

 
char sent[100];
cout << " Entere sentance: ";


double m = 0;
int m = strlen(sent);
for (int i = 0; i < m; i++)
{

}

	system("pause");
	return 0;

}